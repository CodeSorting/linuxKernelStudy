커널 입문자라면 아래 순서대로 읽는 것을 권장합니다: 개발 환경 설정 → 빌드 시스템 → 커널 모듈 → 커널 아키텍처 → 프로세스 관리 → 메모리 관리 → 인터럽트 순서로 진행하면 전체 그림을 잡을 수 있습니다.

# 리눅스 커널이란?
커널은 하드웨어와 소프트웨어 사이의 중재자입니다 — 프로세스 관리, 메모리 할당, 디바이스 제어 등을 담당합니다.

리눅스는 모놀리식(monolithic) 커널이지만, 모듈(module) 시스템으로 유연성을 확보합니다.

커널 코드는 특권 모드(Ring 0)에서 실행되며, 사용자 프로그램(Ring 3)보다 하드웨어에 직접 접근할 수 있습니다.<br>
1. Ring 0 (커널 모드): 최고 권한, 하드웨어 직접 제어
2. Ring 1 (장치 드라이버): 하드웨어 제어 일부 허용
3. Ring 2 (사용자 인터페이스/OS 서비스): 특정 시스템 자원 접근
4. Ring 3 (사용자 모드): 최저 권한, 하드웨어 접근 불가

전 세계 수천 명의 개발자가 참여하는 오픈소스 프로젝트로, C 언어와 일부 어셈블리로 작성됩니다.

서버, 데스크탑, 모바일(Android), 임베디드, 슈퍼컴퓨터까지 거의 모든 플랫폼에서 동작합니다.

# 학습 로드맵
환경 준비 — 리눅스 배포판 설치 후 커널 소스를 내려받고, 빌드 시스템(Kbuild)으로 직접 컴파일해 봅니다.
1. 개발 환경 설정 

모듈 작성 — 간단한 Hello World 커널 모듈을 만들어 insmod/rmmod로 로드·언로드해 봅니다.
2. 커널 모듈 

아키텍처 이해 — 커널의 계층 구조(User Space → System Call → Kernel → Hardware)를 파악합니다.
3. 커널 아키텍처 

핵심 서브시스템 탐구 — 프로세스, 메모리, 파일시스템, 네트워킹 등 주요 서브시스템을 하나씩 학습합니다.

# 설치 및 개발환경 잡기
```BASH
# 필수 빌드 도구
sudo apt update
sudo apt install -y build-essential \
  gcc make git pkg-config \
  flex bison \
  libelf-dev libssl-dev \
  bc libncurses-dev \
  cpio rsync

# 커널 문서 빌드 도구 (선택)
sudo apt install -y python3-sphinx \
  texlive-latex-base texlive-latex-extra

# 추가 유틸리티
sudo apt install -y kmod dwarves \
  sparse ccache
```
- build-essential: gcc, g++, make 등 기본 빌드 도구 모음
- flex, bison: 파서 생성기 (커널 빌드 스크립트에서 사용)
- libelf-dev: BPF, eBPF 프로그램 빌드에 필요
- libssl-dev: 서명된 커널 모듈 빌드에 필요
- bc: 커널 빌드 스크립트의 계산기
- libncurses-dev: menuconfig TUI에 필요
- pkg-config: libelf, openssl 등 라이브러리 존재 여부를 스크립트로 검증할 때 사용
- dwarves: pahole 등 DWARF 디버깅 정보 분석 도구
- sparse: 정적 분석 도구
- ccache: 컴파일러 캐시(Cache)로 재빌드 속도 향상

make mrproper: 잘못된 설정 제거, 초기 상태로 전환<br>
make defconfig: 기본으로 설정<br>
make -j$(nproc) bzImage: 코어 끌어와서 컴파일 -> bzImage라는 결과물이 나옴.

# 시작
```BASH
qemu-system-x86_64 -kernel arch/x86/boot/bzImage -append "console=ttyS0 panic=-1" -nographic
```
화면에 부팅 로그(dmesg)가 주르륵 올라오다가 Kernel panic - not syncing: VFS: Unable to mount root fs... 형태의 에러가 나면서 멈춘다면 정상입니다. 루트 파일시스템(RootFS)을 아직 연결하지 않았기 때문에 커널 코드가 정상 부팅까지만 작동한 상태입니다.

Ctrl + a를 누르고 x를 누르면 QEMU가 종료됨.

# 커널 모듈 만들기
```BASH
# ~/linux 안에서 함.
sudo apt install -y linux-headers-$(uname -r)
mkdir -p ~/kernel_lab/hello
cd ~/kernel_lab/hello
```

```BASH
vim hello.c
```
이후 코드 작성
```c
/* hello.c - 간단한 Hello World 커널 모듈 */
#include <linux/init.h>
#include <linux/module.h>
#include <linux/kernel.h>

static int __init hello_init(void) {
    pr_info("Hello, Kernel World!\n");
    return 0;
}

static void __exit hello_exit(void) {
    pr_info("Goodbye, Kernel World!\n");
}

module_init(hello_init);
module_exit(hello_exit);

MODULE_LICENSE("GPL");
MODULE_DESCRIPTION("Hello World Module");
```

```BASH
vim Makefile
```
이후 Makefile 작성

```BASH
obj-m += hello.o
KDIR := /lib/modules/$(shell uname -r)/build
PWD := $(shell pwd)

all:
	make -C $(KDIR) M=$(PWD) modules

clean:
	make -C $(KDIR) M=$(PWD) clean
```
이후 make를 하면 된다.
근데 wsl 버전은 6.6이고 여기서 다운로드한 커널은 7.0이어서 QEMU로 실행한 다음 거기서 실행할 것이다.

# 최소한의 셸 환경 만들고 넣기
```BASH
# 1. 뼈대가 되어줄 도구 설치
sudo apt install -y busybox-static

# 2. 작업 폴더에서 파일시스템 껍데기 만들기
cd ~/kernel_lab
mkdir -p rootfs/{bin,proc,sys,dev}

# 3. 필수 명령어(busybox)와 우리가 만든 모듈(hello.ko)을 껍데기 안에 넣기
cp /bin/busybox rootfs/bin/
cp hello/hello.ko rootfs/

# 4. 부팅 직후 실행될 스크립트(init) 작성
cat << 'EOF' > rootfs/init
#!/bin/busybox sh
/bin/busybox --install -s /bin
mount -t proc none /proc
mount -t sysfs none /sys
mount -t devtmpfs none /dev

echo "======================================"
echo " Welcome to My Custom Linux Kernel! "
echo "======================================"

# 부팅되자마자 우리가 만든 모듈 로드하기!
insmod /hello.ko

# 커널 로그(dmesg) 마지막 3줄 출력해서 확인하기
dmesg | tail -3

# 우리가 명령어를 칠 수 있게 셸(sh) 띄우기
exec /bin/sh
EOF

# 5. 스크립트에 실행 권한 주고 압축하기
chmod +x rootfs/init
cd rootfs
find . -print0 | cpio --null -ov --format=newc > ../rootfs.cpio
cd ..
```

## 지금까지 한 작업
1. 커널 다운로드 및 조립 (bzImage): 최고급 '자동차 엔진'을 직접 설계도(소스코드)를 보고 뚝딱뚝딱 조립했어.
2. QEMU 준비: 이 엔진을 테스트해 볼 '뼈대(테스트용 프레임)'를 가져왔어.
3. QEMU로 커널 부팅 (Kernel Panic): 뼈대 위에 엔진만 덜렁 올려놓고 시동을 켰어. 엔진은 부릉부릉 잘 돌아가다가, 핸들도 없고 바퀴도 없으니 "어? 나 운전할 사람이 없는데? 더 이상 할 게 없어!" 하면서 뻗어버린 거야. (이게 아까 본 Kernel panic - Unable to mount root fs야.) (파일 시스템 없음)
4. 기본 도구(BusyBox) 챙기기: 리눅스의 수많은 명령어(ls, cd, cat, sh 등)를 하나의 파일로 꽉꽉 압축해 놓은 '맥가이버 칼' 같은 도구가 있어. 이걸 BusyBox라고 불러. 이걸 가져올 거야.
5. 짐 싸기 (압축 파일 만들기): 빈 폴더를 하나 만들고, 그 안에 'BusyBox(명령어 도구)'와 아까 만든 'hello.ko(우리의 첫 모듈)'를 같이 넣어. 그리고 부팅되자마자 실행될 '환영 인사 스크립트'를 적어 넣은 뒤, 하나의 압축 파일(rootfs.cpio)로 꽁꽁 싸맬 거야.
6. 엔진 + 운전석 결합해서 시동 걸기: 아까처럼 QEMU를 켤 건데, 이번에는 옵션에 방금 싼 압축 파일(rootfs.cpio)을 같이 끼워 넣어줄 거야.